using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace EscapeReality.Shrink
{
    /**
     * Enum that indicates wheter the player is:
     * Normal, Shrunk or Morphing at the moment
     */
    public enum State
    {
        Normal,
        Shrunk,
        Morphing
    }
}